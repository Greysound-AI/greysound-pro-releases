## Greysound Pro 0.1.1

- Fixed the macOS universal build failing to launch on supported Intel Macs by pinning the minimum deployment target to macOS 12.
- Added release validation for both arm64 and x86_64 architecture slices and their minimum macOS versions.
